// Top-level build file — MovieFlix TV 4.0.1.
//
// O MovieFlix TV é a ADAPTAÇÃO PARA TV DO MESMO MOVIEFLIX:
// um shell nativo (WebView) que carrega o site oficial e ativa a interface
// de TV que o próprio site já contém em src/tv/*.
plugins {
    id 'com.android.application' version '8.2.1' apply false
}
